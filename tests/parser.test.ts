import { Token, TokenT } from "../src/tokenizer";
import * as ast from "../src/ast";
import "../src/parser";

describe("Select Clause") {
    test("Basic SELECT", () => {
        let text = "SELECT * FROM container";
    })
}
